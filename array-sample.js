// Creating an Array and Initializing with Values
let courses = ["HTML", "CSS", "Javascript", "React", 90, 80];

console.log(courses);

for(let i=0;i<=courses.length;i++) {
    console.log("i value---", courses[i]);
}